<%= form_with(model: wish, local: true) do |form| %>
  <% if wish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wish.errors.count, "error") %> prohibited this wish from being saved:</h2>

      <ul>
      <% wish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   
  
  
  <div class="field form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :wish_title, :class =>'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :description %>
    <%= form.text_area :description, id: :wish_description, :class =>'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :initDate %>
    <%= form.date_select :initDate, id: :wish_initDate, :class =>'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :endDate %>
    <%= form.date_select :endDate, id: :wish_endDate, :class =>'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.submit  :class =>'btn btn-primary' %>
  </div>
<% end %>
